<div class="row">
  <div class="col-md-12">
    <p>Guidelines for topics</p>
    <ul>
      <li>Make it relevant.</li>
      <li>Make it good.</li>
      <li>Use good penmanship.</li>
    </ul>
  </div>

  <%= form_for [topic] do |f| %>
    <div class= "form-group">
      <%= f.label :title %>
      <%= f.text_field :title, class: 'form-control', placeholder: "Enter new Topic name" %>

      <%= f.submit "Save", class: 'btn btn-success' %>
    </div>
  <% end %>
</div>
